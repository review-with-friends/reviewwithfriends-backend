apiVersion: v1
kind: Service
metadata:
  name: bout
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: bout

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bout-deployment
  labels:
    app: bout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bout
  template:
    metadata:
      labels:
        app: bout
    spec:
      containers:
        - name: backend
          image: registry.digitalocean.com/spacedoglabs/backends:bout
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: spacedoglabs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: spacedoglabs

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spacedoglabs-deployment
  labels:
    app: spacedoglabs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spacedoglabs
  template:
    metadata:
      labels:
        app: spacedoglabs
    spec:
      containers:
        - name: backend
          image: registry.digitalocean.com/mob/backend:spacedoglabs
          ports:
            - containerPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: "bout.spacedoglabs.com"
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: bout
                port:
                  number: 80
    - host: "spacedoglabs.com"
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: spacedoglabs
                port:
                  number: 80
